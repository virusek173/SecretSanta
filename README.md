# 🎅 Secret Santa

A Secret Santa gift exchange application with AI integration.

## ✨ Features

- 🎲 Secret Santa assignment algorithm (cyclic graph)
- 🤖 Personalized message generation using OpenAI GPT-4 (Polish language)
- 🎨 Unique Christmas images generated by DALL-E 3
- 📧 Automated HTML email delivery with messages and images
- 🚀 Optimized for inbox delivery (anti-spam features)
- ✅ Input data validation (email, participants)
- 🧪 Unit tests (Jest)
- 🎯 GitHub Actions CI/CD pipeline

## 📋 Requirements

- Node.js >= 18
- TypeScript
- OpenAI account with API key
- SendGrid account (free tier: 100 emails/day)

## 🚀 Installation

1. Clone the repository:
```bash
git clone <repository-url>
cd SecretSanta
```

2. Install dependencies:
```bash
npm install
```

3. Configure environment variables:
```bash
cp .env.example .env
```

Fill in the `.env` file with your credentials:
```env
OPENAI_API_KEY=sk-...
SENDGRID_API_KEY=SG.your_sendgrid_api_key_here
SENDGRID_FROM=your_verified_email@example.com
```

**Getting SendGrid API Key:**
1. Sign up for a free SendGrid account at https://sendgrid.com/free (100 emails/day forever free)
2. Go to Settings → API Keys → Create API Key
3. Choose "Full Access" or "Mail Send" permissions
4. Copy the API key (it only shows once!)
5. **Verify your sender to avoid spam folder:**

   **Option A (Recommended for production):** Domain Authentication
   - Go to Settings → Sender Authentication → Authenticate Your Domain
   - Click "Get Started" and follow the wizard
   - Add the DNS records (CNAME records) to your domain's DNS settings
   - Wait for verification (usually 24-48 hours)
   - ✅ **This significantly improves email deliverability and prevents spam filtering**

   **Option B (Quick, for testing only):** Single Sender Verification
   - Go to Settings → Sender Authentication → Verify Single Sender
   - Enter your email address and complete verification
   - ⚠️ Note: Emails may still go to spam without domain authentication

4. Create participants file:
```bash
cp config/participants.json.example config/participants.json
```

Edit `config/participants.json` and add your participants:
```json
{
  "participants": [
    {
      "name": "John Doe",
      "email": "john@example.com",
      "description": "Loves fantasy books and good coffee"
    }
  ]
}
```

## 📖 Usage

### Run with email sending:
```bash
npm run santa
```

### Dry-run (preview without sending):
```bash
npm run santa:dry-run
```

This will generate messages and images, and save them to the `output/` directory without sending any emails. Each participant will have:
- `{name}_message.txt` - The personalized message (Polish)
- `{name}_image.png` - The generated Christmas image

### Dry-run without images (faster, cheaper):
```bash
npm run santa:dry-run-no-images
```

This mode skips DALL-E image generation, saving time and API costs. Perfect for testing messages only.

### Advanced usage:
```bash
# Dry-run only
node dist/index.js --dry-run

# Skip images only (still sends emails)
node dist/index.js --no-images

# Both flags together
node dist/index.js --dry-run --no-images
```

### Tests:
```bash
npm test                # Run tests
npm run test:watch      # Watch mode
```

### Build:
```bash
npm run build
```

### Linting:
```bash
npm run lint          # Check code style
npm run lint:fix      # Auto-fix linting issues
```

## 🏗️ Project Structure

```
SecretSanta/
├── src/
│   ├── algorithm/
│   │   └── secretSanta.ts         # Assignment algorithm
│   ├── services/
│   │   ├── EmailService.ts        # Email sending (nodemailer)
│   │   ├── LlmService.ts          # Message generation (OpenAI)
│   │   └── ImageService.ts        # Image generation (DALL-E)
│   ├── types/
│   │   └── index.ts               # TypeScript interfaces
│   ├── utils/
│   │   └── validators.ts          # Validators
│   ├── config/
│   │   └── loadConfig.ts          # Configuration loading
│   ├── __tests__/
│   │   └── algorithm/
│   │       └── secretSanta.test.ts # Algorithm tests
│   ├── SecretSantaApp.ts          # Main application
│   └── index.ts                   # Entry point
├── .github/
│   └── workflows/
│       └── ci.yml                 # GitHub Actions CI
├── config/
│   ├── participants.json          # Participants list (gitignored)
│   └── participants.json.example  # Example
├── .env                            # Credentials (gitignored)
├── .env.example                    # Template
└── package.json
```

## 🧪 Tests

The application includes unit tests for the assignment algorithm:

- ✅ Input validation (min. 3 people, no duplicates)
- ✅ Each person gives to exactly one person
- ✅ Each person receives from exactly one person
- ✅ No one draws themselves
- ✅ Randomization of results
- ✅ Support for large groups (50+ people)

Run tests:
```bash
npm test
```

## 🔒 Security

- Credentials stored in `.env` (not committed)
- Email address validation before sending
- Rate limiting between API calls (2s delay)
- Assignment results are not logged in plain text
- All errors are handled and logged

## 📝 Example Output

```
🎅 Secret Santa Application
══════════════════════════════════════════════════
📂 Loading configuration...
✓ Loaded 5 participants

🎲 Performing Secret Santa assignment...
✓ Created 5 assignments

📨 Processing assignments...

[1/5] Processing Anna Kowalska → Jan Nowak
   🤖 Generating message...
   ✓ Message generated
   🎨 Generating image...
   ✓ Image generated
   📧 Sending email to anna.kowalska@example.com...
   ✓ Email sent successfully
   ⏳ Waiting 2s before next request...

...

══════════════════════════════════════════════════
✅ Secret Santa completed successfully!
══════════════════════════════════════════════════
```

## 🎨 Example Email Message

Each person receives an HTML email containing:
- 🎅 Secret Santa header with emoji
- 💌 Personalized AI-generated message
- 🖼️ Unique Christmas image (DALL-E)
- 📝 Gift suggestion based on recipient's interests
- 🔒 Reminder to keep it secret

## 📄 License

MIT

## 🤝 Development

Improvement suggestions:
- [ ] Add exclusions (some people can't draw others)
- [ ] Web interface instead of CLI
- [ ] Drawing history (database)
- [ ] Multi-language support
- [ ] Custom email templates

## 🐛 Bug Reports

If you find a bug, please create an issue in the repository.
